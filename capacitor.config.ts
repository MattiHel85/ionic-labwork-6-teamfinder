import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-labwork-6-teamfinder',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
